# To traverse a string in forward and backward manner.


def forward(string):
    list(string)
    for i in range(len(string)):
        return string[::]


def backword(string):
    list(string)
    for i in range(len(string)):
        return string[::-1]

print(forward("abc"))
print(backword("abc"))
